# Shell scripts run inside a Linux container. A CRLF here is not a formatting preference; it is
# `exec /app/entrypoint.sh: no such file or directory` on a machine that checked out on Windows.
*.sh text eol=lf
Dockerfile text eol=lf
